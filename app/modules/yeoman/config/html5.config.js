'use strict';
angular.module('yeoman').config(['$locationProvider', function($locationProvider) {
  // This is just an example of another configuration file.
  $locationProvider.html5Mode(false);
}]);
